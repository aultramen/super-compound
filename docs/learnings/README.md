# Learnings

Distilled per-topic notes promoted from `docs/LEARNED_KNOWLEDGE.md` overflow
and archive passes, one Markdown file per topic. Searchable via
`node .agent/tools/knowledge-search.mjs "<query>"`. This directory is a
reference shelf, not a second state database: live entries and their Quick
Reference table stay in `docs/LEARNED_KNOWLEDGE.md`.
