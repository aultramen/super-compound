Pass each prospective write through .agent/tools/workflow-admission.mjs.
Before stopping or interruption log unlogged ERR-*/LRN-* via /sc-compound and
update docs/STATE.md; .continue-here.md is a short pointer:
State, Next action, Artifacts lines only.
Refresh run_id: loop-run.mjs show --run <run_id>.
Keep non-authoritative status, run head digest, evidence, reason, next transition.
Never copy approval envelope or confirmation digest.
STATE needs source-write gate; START/RESUME fresh human confirmation.
