# /sc-compound Runtime Contract

Purpose: capture reusable lessons after a verified non-trivial solution.

Confirm evidence first, then record symptoms, root cause, fix, failed attempts, prevention, and related files under `docs/solutions/<category>/`.
