# /sc-compound Runtime Contract

A verified non-trivial solution and root cause: docs/solutions/.
Run knowledge-search then validate-doc-claims; drift routes to knowledge-refresh.

ADAPTIVE_LEARNING_V2 permits only event-derived verified_pattern_v2 with fresh verifier PASS host-attested checker PASS closed findings human approval. Advisory for human-owned documentation never authority. Never self-modify prompts policy budgets verifier framework goals routes.
