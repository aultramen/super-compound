# /sc-explore Runtime Contract

Purpose: resolve fuzzy intent into BRD-ready business context.

Load user request, nearby docs/code, prior brainstorms, and accepted ADRs only if relevant. Use BRD skeleton first. Record objectives, scope, non-goals, business rules, acceptance gates, and `OPEN-*` blockers.
