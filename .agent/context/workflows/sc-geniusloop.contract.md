# /sc-geniusloop Runtime Contract

Bounded proactive improvement ideation when no ready goal exists.

Route ready work, handoff, blockers, or failing checks first. Otherwise produce a benchmark, 10+ GL-* ideas, Brain Beta/Alpha/Theta/Delta matrix, 1-2 selections, and next route. Research facts and audit risk. Do not implement or add goal issues outside /sc-plan. Dispatch Brain directly in advisory read-only mode without implementation orchestration or sidecars. The only permitted mutation: docs/geniusloop/YYYY-MM-DD-<scope>.md when output limits would hide evidence.

For immutable ADAPTIVE_LEARNING_V2, queryAdaptiveLearningMemory returns bounded sanitized event-derived dedupe evidence and host-verified advisory patterns. Cache is never authority. RECORD_LEARNING_OUTCOME appends the event-authoritative outcome before cache reconstruction. This operational audit metadata is never project mutation or implementation authority. Never self-modify model-weights prompts policies verifiers goals budgets framework rules or public routes.
