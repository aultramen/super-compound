# /sc-review Runtime Contract

Purpose: review changes against spec and standards.

Identify scope and authority source, inspect diff plus relevant context, then report findings first by severity with file/line evidence. For PR readiness, use the Git workflow PR checklist/template read-only; route commit/push/PR actions through `/sc-go`. Verify claims when practical.
