# /sc-init Runtime Contract

Purpose: initialize or refresh project context.

Load project config, README/package metadata, test/build commands, and source layout summaries. Suggest config updates instead of guessing. For reload, read only changed rules/workflows/skills and summarize the delta.
